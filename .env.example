# Application Configuration
PORT=80

# Nexus Gateway Configuration
# The Nexus gateway acts as a PostgreSQL-wire-protocol proxy in front of
# per-tenant DuckDB sessions (see github.com/satheeshds/nexus).
NEXUS_HOST=localhost
NEXUS_PORT=5433
NEXUS_DB=lake

# Nexus Control API (required for per-tenant service account rotation)
# The admin API key is sent as X-Admin-API-Key to the nexus-control service.
NEXUS_CONTROL_URL=http://nexus-control:8080
ADMIN_API_KEY=your-admin-api-key

# Paperless-ngx Configuration
PAPERLESS_URL=http://your-paperless-url
PAPERLESS_USERNAME=your-username
PAPERLESS_PASSWORD=your-password

# Google Cloud Configuration
GOOGLE_CLOUD_PROJECT=your-project-id
GOOGLE_CLOUD_LOCATION=us-central1
DOCUMENT_AI_PROCESSOR_ID=your-processor-id
# Path to service account JSON key file (optional if using default credentials)
GOOGLE_APPLICATION_CREDENTIALS=path/to/service-account.json

# Accounting Configuration (optional - bill creation will be skipped if not set)
ACCOUNTING_URL=http://your-accounting-url:8080
ACCOUNTING_USER=admin
ACCOUNTING_PASS=password

# Tika (optional, used for payout XLSX)
TIKA_URL=http://localhost:9998

# LibreOffice parser service (optional, used for payout XLSX when DuckDB cannot read the file)
# LIBREOFFICE_URL=http://localhost:8091
# LIBREOFFICE_DATA_PATH=/data
